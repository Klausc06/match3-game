{
  "name": "match3-game",
  "version": "1.0.0",
  "description": "Match-3 限时竞技赛 — 90秒消除挑战",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve .",
    "prompt:check": "node tools/validate-contract.mjs",
    "validate:levels": "node tools/validate-level-solvability.mjs",
    "test": "node --test tests/unit/*.test.mjs",
    "test:e2e": "node tests/browser-automation-assert.mjs",
    "test:all": "npm run prompt:check && npm run validate:levels && npm test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
